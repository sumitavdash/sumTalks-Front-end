import { LetterArrayPipe } from './letter-array.pipe';

describe('LetterArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new LetterArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
