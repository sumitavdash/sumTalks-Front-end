import { AppearAnimationDirective } from './appear-animation.directive';

describe('AppearAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new AppearAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
